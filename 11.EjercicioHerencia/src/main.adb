with Rectangulo_Model; use Rectangulo_Model;

procedure Main is

begin
   -- Crear clase abstracta figura (sin atributos)


   -- Crear el metodo abstracto calcular area

   -- Crear la subclase rectangulo(Base, Altura) que herede de figura
   R : Rectangulo := (Base => 10, Altura => 10);

   -- Override el metodo calcular area de

   --  Insert code here.
   null;
end Main;
